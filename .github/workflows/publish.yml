name: Publish
on:
  push:
    tags:
      - v*
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Publish
        env:
          USERNAME: irgaly
          TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew publish
